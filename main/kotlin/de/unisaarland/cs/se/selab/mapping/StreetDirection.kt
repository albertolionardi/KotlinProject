package de.unisaarland.cs.se.selab.mapping

/**
 * enum that keeps track of the direction of a street
 */
enum class StreetDirection {
    SOURCE_TARGET,
    TARGET_SOURCE,
    BIDIRECTIONAL,
}
